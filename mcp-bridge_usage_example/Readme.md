Create a .secrets file with your OpenAi key in the format of:
```
MCP_BRIDGE__INFERENCE_SERVER__API_KEY=sk-proj-Wl_Fy ... 8EA
```

Then point your OpenAi end point of your client to
http://localhost:8000/v1
